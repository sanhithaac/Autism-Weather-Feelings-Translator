{
    "name": "autism-weather-feelings-translator",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "client/app",
        "server"
    ],
    "scripts": {
        "install:all": "npm install && cd client/app && npm install && cd ../../server && npm install",
        "build:client": "cd client/app && npm run build"
    },
    "dependencies": {
        "bcryptjs": "^3.0.3",
        "cors": "^2.8.6",
        "dotenv": "^17.2.3",
        "express": "^5.2.1",
        "jsonwebtoken": "^9.0.3",
        "mongoose": "^9.1.6"
    }
}